import React, { useEffect } from 'react';
import { useDispatch, useSelector } from 'react-redux';

export default function RecommendedStories({story}){

    return (<div key={story.id}>


    </div>)
}
